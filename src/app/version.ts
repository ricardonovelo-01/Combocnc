/**
 * UI build stamp — bump when you ship user-visible changes so you can confirm
 * the deployed bundle matches (compare after refresh vs. what the assistant reported).
 */
export const APP_VERSION = '2026.04.09-4';
